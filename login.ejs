<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login to Chat</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<style>
    body{
        background: linear-gradient(to right, #43234f, #b39fb9);
        
    }
    .container{
        background: #d3c9d3;
        width: 50rem;
        height: 30rem;
        border-radius: 5rem;
        align-content: center;
    }
</style>

<body>
    <div class="container mt-5 ">
        <h2 class="text-center ">Login to Chat</h2>
        <br />
        
        <div class="row">
            <div class="col-6 offset-3">
                <form method="POST" action="/login"  novalidate class="needs-validation">

                    <!-- Name -->
                    <div class="mb-3">
                        <label for="name" class="form-label">Name</label>
                        <input name="name" type="text" placeholder="Enter your name" class="form-control" required />
                        <div class="valid-feedback">Looks good!</div>
                        <div class="invalid-feedback">Please enter your name.</div>
                    </div>

                    <!-- Email -->
                    <div class="mb-3">
                        <label for="email" class="form-label">Email</label>
                        <input name="email" type="email" placeholder="Enter your email" class="form-control" required />
                        <div class="valid-feedback">Looks good!</div>
                        <div class="invalid-feedback">Please enter a valid email.</div>
                    </div>

                    <!-- Meeting Code -->
                    <div class="mb-3">
                        <label for="meetingCode" class="form-label">Meeting Code</label>
                        <input name="meetingCode" type="password" placeholder="Enter meeting code" class="form-control" required />
                        <div class="valid-feedback">Looks good!</div>
                        <div class="invalid-feedback">Please enter the meeting code.</div>
                    </div>

                    <!-- Error Handling -->
                    <% if (typeof error !== 'undefined') { %>
                        <p class="text-danger"><%= error %></p>
                    <% } %>

                    <!-- Submit Button -->
                    <button type="submit" class="btn btn-dark w-20">Join Chat</button>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>

